#pragma once

#include <iostream>


class Utility 
{
public:
    bool checkName(const char name[]);
    bool checkYearsOfExperience(int num);
    bool checkSpecialization(const char arr[]);
    bool checkGender(const char arr[]);
    bool checkDateOfBirth(const char arr[]);
private:
};

